function chunkArrayInGroups(arr, size) {
  var newArr = [];
  var i = 0;

  while (i < arr.length) {
    // pushes the sliced array into a new array
    newArr.push(arr.slice(i, i+size));
    i += size;
  }
  return newArr;
}
