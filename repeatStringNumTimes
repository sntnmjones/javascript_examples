function repeatStringNumTimes(str, num) {
  // if num passed in is less than 1, returns ""
  if (num < 1) {
    return str = "";
  }
  
  var newStr = "";
  
  for (var i = 0; i < num; i++) 
    newStr = newStr.concat(str);
    
  return newStr;
}
